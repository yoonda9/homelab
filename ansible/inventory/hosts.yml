---
# GENERATED by scripts/gen_inventory.py (`just gen-inventory`) from
# `tofu output -json`. Edit the Tofu config/outputs, not this file.
all:
  children:
    docker_host:
      hosts:
        docker-host:
          ansible_host: 192.168.1.111
          ansible_user: root
    plex:
      hosts:
        plex:
          ansible_host: 192.168.1.110
          ansible_user: root
