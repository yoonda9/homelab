---
# GENERATED by scripts/gen_inventory.py (`mise run gen-inventory`) from
# `tofu output -json`. Edit the Tofu config/outputs, not this file.
all:
  children:
    docker_host:
      hosts:
        docker-host:
          ansible_host: 192.168.1.111  # tofu output had no live IP yet; static design fallback
          ansible_user: root
    plex:
      hosts:
        plex:
          ansible_host: 192.168.1.110  # tofu output had no live IP yet; static design fallback
          ansible_user: root
